<?xml version="1.0" encoding="utf-8"?>
<!--
  Web.Debug.config — XDT transform applied when Configuration=Debug.
  Development defaults are already in Web.config, so this file is intentionally minimal.
-->
<configuration xmlns:xdt="http://schemas.microsoft.com/XML-Document-Transform">
  <!-- No overrides needed for local development -->
</configuration>
